//Time Delay Loop
//version 1.0
//Last edited 7/2/2013 by Nik
#include <time.h>
#ifndef POKRAND_H
#define POKRAND_H
void waitFor (time_t secs)
{   time_t exitTime;
	 exitTime = time(NULL) + secs;     //To get exit time from current time by adding reqd time to current time.
	 while (time(NULL) < exitTime);
	{
		//	Continuous loop until current time reaches required exit time.
	}
}
#endif